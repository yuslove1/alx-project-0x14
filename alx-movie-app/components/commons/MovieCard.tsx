import React from 'react';

const MovieCard: React.FC = ({}) => {
    return (
        <div className="movie-card">
        <p>movieCard content</p>
        </div>
    );
};

export default MovieCard;